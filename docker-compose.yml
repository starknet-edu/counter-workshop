version: "3.8"
services:
  test:
    image: barretodavid/starknet-dev:2.6.3
    volumes:
      - .:/app
    command: scarb test
  deploy:
    build: .
    volumes:
      - .:/app
    command: npm run deploy
